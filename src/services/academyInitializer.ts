import { Student, ClassSchedule, Plan, PaymentReceipt, BeltColor, StudentStatus } 
    param($m)
    if ($m.Groups[1].Value -match '\.(js|ts);

export const INITIAL_STUDENTS: Omit<Student, 'id'>[] = [];

export const INITIAL_SCHEDULES: Omit<ClassSchedule, 'id'>[] = [];

export const INITIAL_PLANS: Omit<Plan, 'id'>[] = [];

) {
      $m.Value
    } else {
      $m.Groups[1].Value + '.js' + $m.Groups[2].Value
    }
  ;

export const INITIAL_STUDENTS: Omit<Student, 'id'>[] = [];

export const INITIAL_SCHEDULES: Omit<ClassSchedule, 'id'>[] = [];

export const INITIAL_PLANS: Omit<Plan, 'id'>[] = [];


